# CSV 데이터를 Google Sheets로 가져오기 가이드

## 📊 생성된 파일 정보

- **파일명**: `업무추진비_전체데이터.csv`
- **레코드 수**: 636건
- **총 집행액**: 180,797,850원
- **기간**: 2024년 9월 ~ 2025년 9월
- **인코딩**: UTF-8 with BOM (한글 호환)

### 비목별 통계
| 비목 | 건수 | 금액 |
|------|------|------|
| 기관 | 373건 | 87,107,730원 |
| 시책 | 262건 | 93,640,120원 |

## 🚀 Google Sheets로 가져오기 (3가지 방법)

### 방법 1: 웹 인터페이스에서 직접 가져오기 (권장)

1. **Google Sheets 열기**
   - [Google Sheets](https://sheets.google.com) 접속
   - "용인시장_업무추진비_집행내역" 스프레드시트 열기 (기존에 생성한 시트)

2. **CSV 파일 가져오기**
   - 메뉴: `파일` > `가져오기` 클릭
   - `업로드` 탭 선택
   - `업무추진비_전체데이터.csv` 파일 드래그 앤 드롭 또는 선택

3. **가져오기 설정**
   - **가져오기 위치**: "현재 시트 바꾸기" 또는 "새 시트에 삽입" 선택
   - **구분 기호 유형**: "쉼표"
   - **텍스트를 숫자, 날짜로 변환**: ✅ 체크
   - `데이터 가져오기` 클릭

4. **확인 및 서식 조정**
   - 첫 번째 행(헤더)을 굵게 처리
   - 금액 컬럼(G열)을 숫자 형식으로 포맷
   - 필요시 열 너비 조정

### 방법 2: Google Drive에서 바로 열기

1. **Google Drive에 업로드**
   - [Google Drive](https://drive.google.com) 접속
   - `업무추진비_전체데이터.csv` 파일 업로드

2. **Google Sheets로 열기**
   - 업로드된 CSV 파일에서 우클릭
   - `연결 앱` > `Google Sheets`로 열기
   - 자동으로 스프레드시트로 변환됨

3. **서식 조정**
   - 헤더 행 굵게 처리
   - 금액 컬럼 숫자 형식 적용

### 방법 3: 복사-붙여넣기 (간단한 방법)

1. **CSV 파일을 Excel이나 메모장에서 열기**
   - `업무추진비_전체데이터.csv` 파일 열기
   - 모든 데이터 선택 (Ctrl+A)
   - 복사 (Ctrl+C)

2. **Google Sheets에 붙여넣기**
   - Google Sheets 열기
   - A1 셀 선택
   - 붙여넣기 (Ctrl+V)

3. **데이터 정리**
   - 헤더 행 서식 조정
   - 금액 컬럼을 숫자로 변환

## 📋 데이터 컬럼 구조

| 컬럼명 | 설명 | 데이터 타입 | 예시 |
|--------|------|-------------|------|
| 번호 | 연번 | 숫자 | 1, 2, 3... |
| 사용자 | 사용자명 | 문자 | 용인시장 |
| 사용일시 | 사용 날짜/시간 | 날짜시간 | 2024-09-02 10:11:00 |
| 사용장소 | 사용 장소/업체명 | 문자 | 슬로 용인 |
| 집행목적 | 집행 목적 설명 | 문자 | 내방객 접견 다과 등 물품 구입 |
| 대상인원 | 대상 인원 수 | 숫자/문자 | 7, - (해당없음) |
| 사용금액 | 집행 금액 (원) | 숫자 | 119100 |
| 결제방법 | 결제 수단 | 문자 | 카드, 현금, 계좌이체 |
| 비목 | 비목 분류 | 문자 | 기관, 시책 |
| 비고 | 추가 메모 | 문자 | (선택사항) |

## 🔧 서비스 계정 공유 설정

CSV를 Google Sheets로 가져온 후, 웹사이트에서 데이터를 읽을 수 있도록 **서비스 계정에 공유 권한**을 부여해야 합니다.

1. **스프레드시트 공유**
   - 우측 상단 `공유` 버튼 클릭

2. **서비스 계정 이메일 추가**
   - `.env.local` 파일의 `GOOGLE_CLIENT_EMAIL` 값 복사
   - 또는 JSON 키 파일의 `client_email` 값 사용
   - 예: `your-service-account@your-project.iam.gserviceaccount.com`

3. **권한 설정**
   - 권한: **"뷰어"** 선택
   - `완료` 클릭

4. **스프레드시트 ID 복사**
   - URL에서 ID 부분 복사:
   ```
   https://docs.google.com/spreadsheets/d/[이_부분이_SPREADSHEET_ID]/edit
   ```

5. **환경 변수 업데이트**
   - `.env.local` 파일 열기
   - `GOOGLE_SPREADSHEET_ID` 값을 복사한 ID로 업데이트

## ✅ 데이터 확인 체크리스트

가져오기 후 다음 사항을 확인하세요:

- [ ] 총 636개의 데이터 행이 있는가? (헤더 제외)
- [ ] 헤더 행이 첫 번째 행에 있는가?
- [ ] 금액 컬럼이 숫자 형식인가? (쉼표가 자동으로 표시되는가?)
- [ ] 날짜 컬럼이 날짜 형식으로 인식되는가?
- [ ] 한글이 깨지지 않고 정상적으로 표시되는가?
- [ ] 서비스 계정이 스프레드시트에 공유되었는가?

## 🎯 웹사이트에서 데이터 확인

1. **개발 서버 실행**
   ```bash
   cd yongincity_business_expenses
   npm run dev
   ```

2. **브라우저에서 확인**
   - http://localhost:3000 접속
   - 데이터가 정상적으로 로드되는지 확인
   - 총 집행액: 180,797,850원
   - 총 건수: 636건
   - 월 평균: 약 13,907,527원 (13개월 기준)

3. **오류 발생 시**
   - 브라우저 개발자 도구 콘솔 확인 (F12)
   - 서버 로그 확인
   - 환경 변수가 올바르게 설정되었는지 확인
   - 서비스 계정 권한 확인

## 📊 데이터 미리보기

### 첫 5개 레코드 예시

| 번호 | 사용자 | 사용일시 | 사용장소 | 집행목적 | 대상인원 | 사용금액 | 결제방법 | 비목 |
|------|--------|----------|----------|----------|----------|----------|----------|------|
| 1 | 용인시장 | 2024-09-02 10:11 | 슬로 용인 | 내방객 접견 다과 등 물품 구입 | - | 119,100 | 카드 | 시책 |
| 2 | 용인시장 | 2024-09-02 15:09 | SSG.COM | 내방객 접견 다과 등 물품 구입 | - | 206,920 | 카드 | 시책 |
| 3 | 용인시장 | 2024-09-02 16:41 | 샐러디 용인역북점 | 내방객 접견 다과 등 물품 구입 | - | 57,000 | 카드 | 시책 |
| 4 | 용인시장 | 2024-09-03 11:06 | 포케올데이용인역북점 | 업무보고조정 및 수행직원 격려 | 7 | 109,200 | 카드 | 기관 |
| 5 | 용인시장 | 2024-09-03 20:01 | 아낙감 | 시정홍보 관련 언론관계자 업무협의 | 4 | 105,000 | 카드 | 시책 |

## 🔄 데이터 업데이트 방법

### 새로운 데이터 추가 시

1. **Excel 파일 업데이트**
   - `docs` 폴더의 Excel 파일에 새 데이터 추가

2. **데이터 재추출**
   ```bash
   python extract_excel_data.py
   ```

3. **CSV를 Google Sheets에 다시 가져오기**
   - 위의 가져오기 방법 중 하나 사용
   - "현재 시트 바꾸기" 선택하여 기존 데이터 교체

4. **웹사이트 자동 갱신**
   - 1시간 후 자동으로 새 데이터 반영 (revalidate 설정)
   - 즉시 반영 필요시: Vercel Dashboard에서 "Redeploy"

## ⚠️ 주의사항

1. **인코딩 문제**
   - CSV 파일은 UTF-8 BOM으로 저장되어 한글을 지원합니다
   - Excel에서 열 때 깨지는 경우: "데이터" > "텍스트 나누기"에서 UTF-8 선택

2. **날짜 형식**
   - 일부 날짜에 시/분/초가 포함되어 있습니다
   - Google Sheets에서 자동으로 인식되지 않으면 수동으로 날짜 형식 적용

3. **금액 형식**
   - 쉼표 없이 숫자만 저장되어 있습니다
   - Google Sheets에서 숫자 형식을 적용하면 자동으로 쉼표 표시

4. **비목 분류**
   - 일부 레코드에서 비목이 공백인 경우가 있을 수 있습니다
   - 필요시 수동으로 "기관" 또는 "시책"으로 분류

## 🆘 문제 해결

### Q: Google Sheets에서 한글이 깨집니다
A:
- CSV 파일이 UTF-8 BOM으로 저장되었는지 확인
- Google Drive에 업로드 후 Google Sheets로 열기 방법 사용
- 또는 "방법 1"의 가져오기 기능 사용

### Q: 금액이 숫자로 인식되지 않습니다
A:
- 금액 컬럼 선택 후 `표시 형식` > `숫자` > `숫자` 적용
- 또는 `표시 형식` > `숫자` > `통화` 적용하여 원화 표시

### Q: 날짜 형식이 이상합니다
A:
- 날짜 컬럼 선택 후 `표시 형식` > `숫자` > `날짜 시간` 적용
- 원하는 날짜 형식을 `사용자 지정 날짜 및 시간 형식`에서 설정

### Q: 웹사이트에서 데이터가 로드되지 않습니다
A:
1. 서비스 계정이 스프레드시트에 공유되었는지 확인
2. `.env.local` 파일의 `GOOGLE_SPREADSHEET_ID` 확인
3. 스프레드시트 시트 이름이 "Sheet1"인지 확인 (또는 코드에서 수정)
4. `src/lib/googleSheets.ts`의 `RANGE` 설정 확인

## 📚 추가 리소스

- [Google Sheets API 공식 문서](https://developers.google.com/sheets/api)
- [프로젝트 README.md](./README.md) - 전체 프로젝트 설정 가이드
- [GOOGLE_SHEETS_SETUP.md](./GOOGLE_SHEETS_SETUP.md) - Google Sheets 상세 설정 가이드
